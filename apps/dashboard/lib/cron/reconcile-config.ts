/** Run counter drift threshold: |delta| above this is classified as abnormal */
export const ABNORMAL_RUN_DRIFT_THRESHOLD = 10;

/** Quota counter drift threshold: |delta| above this is classified as abnormal */
export const ABNORMAL_QUOTA_DRIFT_THRESHOLD = 5;
