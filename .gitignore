# Dependencies
node_modules/

# Build output
dist/
.next/
out/

# TypeScript incremental build cache
*.tsbuildinfo

# Environment files (all variants except the example template)
.env
.env.local
.env.*
!.env.example

# Turborepo cache
.turbo/

# Playwright test artifacts
test-results/
playwright-report/

# Playwright auth state (contains live session JWTs — never commit)
**/e2e/.auth/
e2e-smoke/.auth/
e2e-smoke/.smoke-state.json

# Test coverage
coverage/
*.lcov

# npm pack artifacts
*.tgz

# ESLint cache
.eslintcache

# Supabase CLI local state
supabase/.temp/

# macOS
.DS_Store

# Claude Code (local state — settings.local.json, sessions, hooks, etc.)
.claude/

# Internal docs and workflow (not for public repo)
/docs/
openspec/

# Vercel
.vercel
.env*.local
