---
"@llmassert/playwright": patch
---

Add `.env.example` documenting all environment variables for npm consumers (judge API keys, reporter config, debug flags) and include it in the published tarball
